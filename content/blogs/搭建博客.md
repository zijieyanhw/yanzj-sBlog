---
title: "使用 Hugo Profile 搭建博客"
date: 2026-01-06T14:46:16.388Z
draft: false
author: "yanzj"
tags:
  - Hugo Profile
image: /images/post.jpg
description: ""
toc: true
---

## 简介
该博客记录使用 [Hugo Profile](https://github.com/gurusabarish/hugo-profile) 搭建博客的过程。

## 涉及工具
- [PowerShell 7 (x64)](https://learn.microsoft.com/zh-cn/powershell/scripting/install/install-powershell-on-windows?view=powershell-7.5#install-the-msi-package)
- hugo
- [Hugo Profile](https://github.com/gurusabarish/hugo-profile)
- git
- [github](https://github.com/)

## 基础项目搭建
### PowerShell安装
下载安装包点击下一步安装即可

### git安装
[详见该文档](https://blog.csdn.net/mukes/article/details/115693833)

### hugo安装
在PowerShell中执行下列代码安装Scoop
```
# 设置 PowerShell 执行策略
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# 下载安装脚本
irm get.scoop.sh -outfile 'install.ps1'
# 执行安装, --ScoopDir 参数指定 Scoop 安装路径
.\install.ps1 -ScoopDir 'C:\Scoop'
```
再利用Scoop安装hugo
```
scoop install hugo-extended
```

## 初始化项目
在PowerShell中执行下列代码
```
hugo new site quickstart // 创建一个新网站
cd quickstart // 切换到该目录
git init // git初始化
git submodule add https://github.com/theNewDynamic/gohugo-theme-ananke.git themes/ananke // 添加主题
echo "theme = 'ananke'" >> hugo.toml // 修改配置文件
hugo server // 编译启动
```

## 将项目部署到github
首先在你的github主页上新建一个仓库(new repository)

在你的hugo目录下执行下面的配置代码
```
git remote add origin https://github.com/xxxx/xxxx.git // 使用自己的仓库地址
git branch -M main
git push -u origin main
```

在项目根目录创建 <mark>.github\workflows\hugo.yaml</mark> 文件，将下面的内容复制到文件内。
```
# 该文件是github官方的构建 Hugo 网站并将其部署到 GitHub Pages 的自动工作流程
# 只需修改版本分支(branches: main)，hugo版本号(HUGO_VERSION: 0.154.2)与本地一致即可
# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Deploy Hugo site to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

# Default to bash
defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.154.2
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
      - name: Install Dart Sass
        run: sudo snap install dart-sass
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"
      - name: Build with Hugo
        env:
          HUGO_CACHEDIR: ${{ runner.temp }}/hugo_cache
          HUGO_ENVIRONMENT: production
        run: |
          hugo \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```






